meta {
  name: Report User Badges (Auth via API Key)
  type: http
  seq: 3
}

get {
  url: {{HOST}}/api/v2/badges.php
  body: none
  auth: bearer
}

headers {
  Accept: text/csv
}

auth:bearer {
  token: {{API_KEY}}
}

settings {
  encodeUrl: true
  timeout: 0
}
