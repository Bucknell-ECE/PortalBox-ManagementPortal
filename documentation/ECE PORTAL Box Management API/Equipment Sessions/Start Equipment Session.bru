meta {
  name: Start Equipment Session
  type: http
  seq: 1
}

put {
  url: {{HOST}}/api/v2/session.php?id={{MAC_ADDRESS}}
  body: none
  auth: inherit
}

params:query {
  id: {{MAC_ADDRESS}}
}

settings {
  encodeUrl: true
}
