meta {
  name: Deactivate
  type: http
  seq: 7
}

post {
  url: {{HOST}}/api/v2/box-activation.php?mac={{MAC_ADDRESS}}
  body: none
  auth: bearer
}

params:query {
  mac: {{MAC_ADDRESS}}
}

auth:bearer {
  token: {{CARD_ID}}
}

settings {
  encodeUrl: true
}
