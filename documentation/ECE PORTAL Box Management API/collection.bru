meta {
  name: ECE PORTAL Box Management API
}

auth {
  mode: none
}

vars:pre-request {
  host: ${YOUR_TEST_SERVER:PORT}
  auth_cookie: PHPSESSID=${YOUR_SESSION_COOKIE}
  google_id_token: ${YOUR_GOOGLE_OAUTH2_ID_TOKEN}
}

docs {
  This API implements a RESTful interface to the PORTAL Box Backend. It uses OAuth2 along with a session cookie for authentication see login.php.
}
