info:
  name: Read Role errors when id is not int
  type: http
  seq: 14

http:
  method: GET
  url: "{{HOST}}/api/v2/roles.php?id=Admin"
  params:
    - name: id
      value: Admin
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body
      operator: eq
      value: The role id must be specified as an integer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
