info:
  name: Update Role errors when id is not specified
  type: http
  seq: 18

http:
  method: POST
  url: "{{HOST}}/api/v2/roles.php"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body
      operator: eq
      value: You must specify the role to modify via the id param

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
